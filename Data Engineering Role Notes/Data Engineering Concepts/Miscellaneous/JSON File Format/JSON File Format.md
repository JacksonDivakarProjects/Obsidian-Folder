JSON (JavaScript Object Notation) is the most common text-based data interchange format in data engineering — used for API payloads, config files, log records, and semi-structured storage. Notes in this section:

- [[Types of JSON|Types of JSON]] — the different JSON document shapes you'll encounter (object, array, nested, NDJSON/JSON Lines, JSON Schema, etc.) and when each is used.
- [[JSON with Python|JSON with Python]] — reading and writing JSON with Python's `json` module, plus flattening nested JSON into tables with `pandas.json_normalize()`.
