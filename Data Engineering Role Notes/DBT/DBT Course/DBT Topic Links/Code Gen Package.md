dbt Learn bookmark — Fundamentals course, "Understanding Sources" lesson: https://learn.getdbt.com/learn/course/dbt-fundamentals-vs-code/sources-60min/understanding-sources?page=7

Covers the `dbt-labs/codegen` package, which auto-generates `source()` YAML blocks and boilerplate staging model SQL from existing database tables — useful for skipping the hand-written boilerplate when onboarding a new raw source. Install it like any dbt package, via `packages.yml`.
